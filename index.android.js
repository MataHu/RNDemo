import { AppRegistry } from 'react-native'

import {
    TK,
    SecKill
  } from './app/views/Views'
console.disableYellowBox = true

AppRegistry.registerComponent('TK', () => TK);
AppRegistry.registerComponent('SecKill', () => SecKill);
